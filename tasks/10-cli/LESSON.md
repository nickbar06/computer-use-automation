# Lesson 10 — Commands reviewers will paste

**Do:** Make `npm run cua -- help` match what you will later put in the repo README.

**Check you understand:**

- Replay must work with no API key.
- `business_outcome` is exit 0; only `failed` is a hard CLI failure.
- `--confirm` is how irreversible replay is allowed unattended.

Next: [../11-tests/README.md](../11-tests/README.md)
