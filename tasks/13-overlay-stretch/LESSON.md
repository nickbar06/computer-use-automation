# Lesson 13 — Reuse, don’t re-record

**Do:** Only after Task 12 is `done`. Prove Northlake’s “Search Member” and Lakecrest’s “🔍 Search” need overlays, and that without them you do not silently succeed.

**Check you understand:**

- Artifacts are keyed by vendor product, not by tenant.
- Emoji in a button name is the same overlay as a renamed label — still `role_name` on `find_member`.
- When names diverge past an overlay, fail a checkpoint / escalate — do not click the wrong control.

If you skip this task, mention it under REPORT heading 7 (Cuts).
