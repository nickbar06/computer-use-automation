# Lesson 01 — A package, not an agent yet

**Read:** nothing extra. The scaffold exists so later tasks have `npm run cua`.

**Do:** finish [README.md](README.md). Run `npm install` and `npx tsc --noEmit`.

**Check you understand:**

- Why the CLI is a stub (`serve` can no-op until Task 02).
- Why `.ts` imports need `allowImportingTsExtensions` when using `tsx` + `noEmit`.

Next: [../02-mock-core/README.md](../02-mock-core/README.md)
